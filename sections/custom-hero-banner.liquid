{% comment %}
  Aayu Pain - 100% Full-Width Brand Hero Banner Section (Zero Gap)
{% endcomment %}

<div class="aayu-hero-section section-{{ section.id }}-padding">
  <div class="aayu-hero-fullwidth">
    <div class="aayu-hero-wrapper">
      {%- if section.settings.image != blank -%}
        <div class="aayu-hero-media">
          <img
            srcset="{{ section.settings.image | image_url: width: 750 }} 750w, {{ section.settings.image | image_url: width: 1500 }} 1500w, {{ section.settings.image | image_url }} 2000w"
            src="{{ section.settings.image | image_url: width: 1500 }}"
            alt="{{ section.settings.heading | escape }}"
            loading="eager"
            width="{{ section.settings.image.width }}"
            height="{{ section.settings.image.height }}"
            class="aayu-hero-img"
          >
        </div>
      {%- else -%}
        <!-- High Quality Built-in AyuPain Banner Presentation (Full Width Edge-to-Edge) -->
        <div class="aayu-hero-banner-card">
          <div class="aayu-banner-grid">
            <div class="aayu-banner-text-col">
              <span class="aayu-hero-subtag">🌿 100% NATURAL & AYURVEDIC FORMULA</span>
              <h1 class="aayu-hero-title">AyuPain™ Pain Relief Oil</h1>
              <p class="aayu-hero-hindi">आयुर्वेदिक दर्द राहत तेल - 100% आयुर्वेदिक</p>
              <p class="aayu-hero-desc">Fast action deep penetration formula for Joint, Knee, Back, Shoulder & Muscle Pain Relief. Formulated with 48+ powerful herbal oils.</p>
              
              <div class="aayu-hero-bullets">
                <div class="aayu-hero-bullet-item">✓ Fast Action Relief</div>
                <div class="aayu-hero-bullet-item">✓ Deep Penetration</div>
                <div class="aayu-hero-bullet-item">✓ No Chemicals & Side Effects</div>
              </div>

              <div class="aayu-hero-actions">
                <a href="{{ section.settings.button_link }}" class="button button--primary aayu-hero-cta btn-cod-pulsing">
                  {{ section.settings.button_label | default: "ORDER CASH ON DELIVERY (COD)" }}
                </a>
              </div>
            </div>
            
            <div class="aayu-banner-features-col">
              <div class="aayu-relief-pill">🦵 Knee Pain Relief</div>
              <div class="aayu-relief-pill">🦴 Back Pain Relief</div>
              <div class="aayu-relief-pill">💪 Shoulder & Neck Care</div>
              <div class="aayu-relief-pill">🏃 Muscle Stiffness Relief</div>
              <div class="aayu-relief-pill">✨ Joint & Arthritis Care</div>
            </div>
          </div>
        </div>
      {%- endif -%}
    </div>
  </div>
</div>

<style>
  .aayu-hero-section {
    margin: 0 !important;
    padding: 0 !important;
    width: 100% !important;
  }
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
  .aayu-hero-fullwidth {
    width: 100%;
    max-width: 100%;
    margin: 0;
    padding: 0;
  }
  .aayu-hero-wrapper {
    width: 100%;
    overflow: hidden;
    margin: 0;
    padding: 0;
  }
  .aayu-hero-media img {
    width: 100%;
    height: auto;
    display: block;
    object-fit: cover;
  }
  .aayu-hero-banner-card {
    background: linear-gradient(135deg, #093f25 0%, #0e5a36 60%, #166534 100%);
    color: #ffffff;
    padding: 4rem 5%;
    position: relative;
    border-bottom: 3px solid #cca43b;
    width: 100%;
  }
  .aayu-banner-grid {
    display: grid;
    grid-template-columns: 1.4fr 0.8fr;
    gap: 4rem;
    align-items: center;
    max-width: 1400px;
    margin: 0 auto;
  }
  @media screen and (max-width: 850px) {
    .aayu-banner-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .aayu-hero-banner-card {
      padding: 2.5rem 1.5rem;
    }
  }
  .aayu-hero-subtag {
    display: inline-block;
    background: rgba(204, 164, 59, 0.2);
    border: 1px solid #cca43b;
    color: #cca43b;
    font-size: 1.2rem;
    font-weight: 800;
    padding: 4px 14px;
    border-radius: 20px;
    margin-bottom: 1.2rem;
    letter-spacing: 1px;
  }
  .aayu-hero-title {
    font-size: 4rem;
    font-weight: 800;
    color: #ffffff;
    margin: 0 0 0.5rem 0;
    line-height: 1.15;
  }
  @media screen and (max-width: 640px) {
    .aayu-hero-title {
      font-size: 2.8rem;
    }
  }
  .aayu-hero-hindi {
    font-size: 2.2rem;
    font-weight: 700;
    color: #cca43b;
    margin: 0 0 1.5rem 0;
  }
  .aayu-hero-desc {
    font-size: 1.6rem;
    color: #e5e7eb;
    line-height: 1.6;
    margin: 0 0 2rem 0;
  }
  .aayu-hero-bullets {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 2.5rem;
  }
  .aayu-hero-bullet-item {
    background: rgba(255,255,255,0.12);
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 1.35rem;
    font-weight: 600;
    color: #ffffff;
  }
  .aayu-hero-cta {
    background: linear-gradient(135deg, #cca43b 0%, #b38b2d 100%) !important;
    color: #ffffff !important;
    font-size: 1.5rem !important;
    font-weight: 800 !important;
    padding: 1.4rem 2.8rem !important;
    border-radius: 8px !important;
    border: none !important;
    box-shadow: 0 4px 20px rgba(204, 164, 59, 0.4);
    text-transform: uppercase;
    text-decoration: none;
    display: inline-block;
  }
  .aayu-banner-features-col {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .aayu-relief-pill {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(204, 164, 59, 0.4);
    padding: 14px 20px;
    border-radius: 12px;
    font-size: 1.5rem;
    font-weight: 700;
    color: #ffffff;
    display: flex;
    align-items: center;
    gap: 12px;
    transition: background 0.2s ease;
  }
  .aayu-relief-pill:hover {
    background: rgba(204, 164, 59, 0.2);
  }
</style>

{% schema %}
{
  "name": "Aayu Pain Hero Banner",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Banner Image (Upload your custom banner image here)"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "AyuPain™ Pain Relief Oil"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "ORDER CASH ON DELIVERY (COD)"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link",
      "default": "/collections/all"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top Padding",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "Aayu Pain Hero Banner"
    }
  ]
}
{% endschema %}
